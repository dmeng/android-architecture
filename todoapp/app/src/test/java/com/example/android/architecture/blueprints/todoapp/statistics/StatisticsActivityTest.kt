package com.example.android.architecture.blueprints.todoapp.statistics

import org.junit.Assert.fail
import org.junit.Test

class StatisticsActivityTest {
  @Test fun selectMenuItem_opensDrawer() = fail("Unimplemented")

  // So I see the add/edit screen sets it up in the fragment and not the activity.
  // Can we be consistent?
  @Test fun onCreate_setsUpActionBar() = fail("Unimplemented")
  @Test fun onCreate_setsUpUi() = fail("Unimplemented")
  // This will be an instrumented test in StatisticsScreenTest.
  // Keeping it here for now for easy reference
  @Test fun navigateToTasksScreen() = fail("Unimplemented")
}