package com.example.android.architecture.blueprints.todoapp.tasks

import org.junit.Assert.fail
import org.junit.Test

class TasksFragmentTest {
  @Test fun onActivityCreate_setsUpButton() = fail("Unimplemented")
  @Test fun onActivityCreate_setsUpListAdapter() = fail("Unimplemented")
  // Since the options menu is where I tore my hair the most, may as well make sure that we
  // actually inflate the menu when we say we will
  @Test fun onCreateOptionsMenu() = fail("Unimplemented")
  @Test fun onResume_startsViewModel() = fail("Unimplemented")
}